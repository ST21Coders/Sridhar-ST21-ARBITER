# Personal Claude Code memory — keep local, do not share
CLAUDE.local.md
.claude/settings.local.json

# Python venv created by scripts/ workflow (PEP 668 workaround)
scripts/.venv/
scripts/__pycache__/
**/__pycache__/
*.pyc

# SAM build cache
Infra/.aws-sam/

# UI build artifacts
ui/node_modules/
ui/dist/

# Real per-env Vite values — generated by post_deploy_ui.py from CFN exports.
# Keep .env.*.example committed as templates; the real values stay local.
ui/.env
ui/.env.local
ui/.env.development
ui/.env.production
ui/.env.*.local

# Plan files / scratch
.claude/plans/
.aws-sam/

# Pip wheels, egg-info
*.egg-info/
*.whl

# Coverage reports
ui/coverage/
.coverage
htmlcov/

# CloudFormation packaging artifacts
packaged-*.yaml
packaged-*.yml

# OS / editor noise
.DS_Store
.idea/
.vscode/
*.swp
*.lock
