# UI environment variables (dev).
# Copy to ui/.env.development (loaded by `npm run dev` / Vite dev server).
#
# Values reflect the CURRENT dev deployment in us-east-1. If you re-deploy
# 06-api or 03-identity, refresh by running:
#   aws cloudformation describe-stacks --stack-name dev-lmarbiter-06-api \
#     --query 'Stacks[0].Outputs[?OutputKey==`UIEnvHints`].OutputValue' --output text
#
# Mapping (same as .env.production.example, just with dev values):
#   VITE_API_URL           06-api → ApiEndpoint           (all routes except /chat)
#   VITE_CHAT_URL          06-api → ChatFunctionUrl       (Lambda Function URL — /chat only)
#   VITE_COGNITO_*         03-identity stack outputs
#
# NOTE: /chat hits the Function URL with AuthType=NONE (demo). The rest of
# the API still requires a Cognito JWT.

VITE_API_URL=https://w6zdn6htjd.execute-api.us-east-1.amazonaws.com/dev
VITE_CHAT_URL=https://lzynm434y3jjpraqjj2kqvac5i0kvwiq.lambda-url.us-east-1.on.aws/

VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=us-east-1_h7SqhQYik
VITE_COGNITO_CLIENT_ID=7mkf2q2m0hunc2idluoddik4ku
VITE_COGNITO_DOMAIN=dev-lmarbiter.auth.us-east-1.amazoncognito.com
# No CloudFront yet — for `npm run dev` use the Vite default origin:
VITE_COGNITO_REDIRECT_URI=http://localhost:5173/callback
VITE_COGNITO_LOGOUT_URI=http://localhost:5173/
