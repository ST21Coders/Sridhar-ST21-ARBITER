# AgentCore Runtime container — listens on :8080 for /invocations and /ping
FROM public.ecr.aws/docker/library/python:3.13-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY agent.py .
# scan_rule_pack ships the 12 deterministic UC matchers used in scan-mode.
COPY scan_rule_pack.py .

EXPOSE 8080
# opentelemetry-instrument wraps the process so the AWS OTel distro
# auto-instruments boto3, requests, and the AgentCore SDK. Traces flow
# to the Console's AgentCore Observability tab.
CMD ["opentelemetry-instrument", "python", "agent.py"]
