bedrock-agentcore>=0.1.0
strands-agents>=0.1.0
boto3>=1.34.0
# AWS distro of OpenTelemetry — enables AgentCore Observability traces in
# the Console. Used together with `opentelemetry-instrument` in the Dockerfile CMD.
aws-opentelemetry-distro>=0.12.0
